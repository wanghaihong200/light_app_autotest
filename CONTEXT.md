# 轻应用自动化（light_app_autotest）

产品线 UI 自动化测试框架的领域术语表。被测对象是本产品线的多个混合 App 与微信小程序；业务流程用例跨端复用，页面实现按端各写一份。

## Language

### 被测对象

**产品线**:
公司的一个业务产品族，包含多个 App 及其对应的微信小程序，是本框架被测对象的全集。
_Avoid_: 业务线

**混合 App**:
产品线下的原生 Android App，含大量 WebView 页面。
_Avoid_: 原生 App

**小程序**:
运行在微信宿主内的产品线轻应用，与混合 App 是同一业务的不同端。
_Avoid_: H5（H5 特指混合 App 内的 WebView 页面）

**端**:
一个具体的被测目标，由产品、OS 和页面载体共同确定（如"XX App-Android"、"XX 小程序"、"XX App-鸿蒙"）。业务流程跨端复用；页面实现按端各写一份。
_Avoid_: 平台、Surface

**鸿蒙端**:
现有混合 App 移植到鸿蒙后形成的新端，含 ArkWeb 页面。现阶段只要求驱动接口能插入鸿蒙驱动，不实现。

### 用例模型

**业务流程**:
端无关的业务编排（如"下单 = 加购→结算→支付成功"），全产品线只写一份。
_Avoid_: 场景、Test Flow

**页面实现**:
业务流程中一个页面在某端的具体实现（定位与操作），对上暴露统一接口。
_Avoid_: PO、PageObject（易被误解为仅限 App 端）

**驱动**:
某一端的底层自动化引擎适配层，每端恰有一个（混合 App 驱动、小程序驱动、未来的鸿蒙驱动）。页面实现不感知底层引擎。
_Avoid_: 引擎、内核

### 小程序运行环境

**一级环境**:
小程序用例的正式运行环境（微信开发者工具），进 CI；结果按"工具渲染"口径解读。

**探索环境**:
手工触发的真机调试通道，仅用于排查工具/真机渲染差异，不进 CI、不作质量门禁。

### 定位与证据

**定位器**:
页面实现中一个控件的定位描述，集中在 Page 层管理，禁止散落在用例内。开发团队不在控件上打标，故定位器必须支持多级兜底。

**候选链**:
定位器的多级候选策略（resource-id → 文本 → 层级 xpath，依次尝试），失败时输出完整尝试轨迹。

**失败现场包**:
用例失败时自动收集的现场材料：截图、录屏、App 源树 dump、小程序 page.data dump。
